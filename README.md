# Отчёт МТС → ВК — четыре варианта под разные устройства

Проект автоматически формирует рабочий отчёт по данным портала
`inventory.ural.mts.ru` и отправляет его в чат ВКонтакте. Есть несколько
реализаций — выберите под своё устройство:

| Вариант | Где | От чьего имени | Автомат | Папка |
|---|---|---|---|---|
| Расширение | Компьютер (Яндекс/Chrome) | ваш аккаунт | полный | этот каталог |
| Приложение | Android | ваш аккаунт | полный | [`android/`](android/) |
| Скрипты + MacroDroid | Android | ваш аккаунт | полный | [`mobile/android-auto/`](mobile/android-auto/) |
| Полу-ручной скрипт | Телефон | ваш аккаунт | по кнопке | [`mobile/`](mobile/) |
| Облако + бот | Любое, вкл. iPhone | бот-сообщество | полный | [`automation/`](automation/) |

Ниже — про **расширение для компьютера**. Остальные варианты описаны в своих
папках (ссылки в таблице).

📱 **Готовый APK Android-приложения** — на странице
[**Releases**](../../releases/latest) (файл `otchet-mts-vk.apk`).

---

# VK Авто-отчёт — расширение для Яндекс Браузера

Расширение автоматически отправляет отчёт (текст по шаблону) в выбранный чат
ВКонтакте по расписанию — например, ежедневный рабочий отчёт в 22:00 по будням.
Также можно отправить отчёт вручную кнопкой «Отправить сейчас».

Работает через веб-версию vk.com: в назначенное время расширение открывает
нужный чат в фоновой вкладке, вставляет текст в поле ввода и отправляет его.
Токены и API ВК не используются — нужен только выполненный вход в ВК в браузере.

## Установка в Яндекс Браузере

1. Скачайте/склонируйте эту папку на компьютер.
2. Откройте в браузере адрес `browser://extensions` (или Меню → Дополнения →
   внизу «Каталог расширений» → значок настроек).
3. Включите переключатель **«Режим разработчика»** (справа сверху).
4. Нажмите **«Загрузить распакованное расширение»** и укажите папку проекта
   (ту, где лежит `manifest.json`).
5. Значок расширения появится на панели. Закрепите его для удобства.

## Настройка

Нажмите на значок расширения:

| Поле | Что указывать |
|---|---|
| Чат | id получателя из адресной строки: откройте нужный чат на `vk.com/im` (или `vk.ru/im`) — в адресе будет `?sel=…`. Число — личный диалог, `c15` — беседа №15, можно короткое имя (`durov`). Внутренний id беседы вида `2000000066` тоже подойдёт — он преобразуется в `c66` автоматически |
| Время отправки | во сколько отправлять отчёт |
| Дни недели | по умолчанию Пн–Пт |
| Шаблон отчёта | текст сообщения; `{date}` заменится на дату, `{time}` — на время |

Включите галочку **«Автоотправка включена»** и нажмите **«Сохранить»**.

## Как происходит отправка

Расширение открывает (или активирует) вкладку с чатом, находит поле ввода
и печатает текст через протокол DevTools (`chrome.debugger`) — для страницы
это неотличимо от настоящего набора с клавиатуры, поэтому способ работает
с любым редактором ВК. На время отправки вверху окна появляется полоска
«Расширение "VK Авто-отчёт" запустило отладку этого браузера» — это
нормально, она исчезает после отправки. Результат (успех или текст ошибки)
показывается системным уведомлением.

## Важно

- В момент отправки браузер должен быть запущен, а в ВК выполнен вход.
- Во время отправки окно с чатом становится активным — не печатайте в этот
  момент в другом поле этого же окна.
- Расширение защищено от дублей: отчёт уходит не более одного раза в день.
- ВК периодически меняет вёрстку сайта. Расширение сначала пробует известные
  селекторы, а если их нет — ищет поле ввода и кнопку «Отправить» эвристически.
  Если отправка всё равно не работает, откройте чат, нажмите F12 → вкладка
  «Консоль» и повторите отправку: расширение пишет туда, какой элемент нашло
  и на каком шаге остановилось. Эти строки помогут поправить селекторы в
  `content.js`.
- После обновления файлов расширения нажмите «Обновить» на
  `browser://extensions` и перезагрузите вкладку с ВК.
- Используйте расширение для своих чатов и с разумной частотой — массовая
  автоматическая рассылка нарушает правила ВКонтакте.

## Использование на телефоне

Полноценное расширение на телефоне не работает: мобильные браузеры не дают
расширениям фоновое расписание и «настоящий» ввод клавиатуры, а установить
распакованное расширение на Android нельзя. Вместо этого есть скрипт
`mobile/mts-vk-report.user.js` для Tampermonkey — почти то же самое,
но отправка подтверждается вручную (два касания).

Установка (Яндекс Браузер на Android):

1. Откройте в мобильном Яндекс Браузере магазин расширений Chrome и
   установите **Tampermonkey**.
2. Откройте меню Tampermonkey → «Создать новый скрипт», удалите заготовку
   и вставьте содержимое файла `mobile/mts-vk-report.user.js`. Сохраните.
3. Откройте страницу `inventory.ural.mts.ru/pc/agent_day.php` — справа
   внизу появится кнопка **«📋 Отчёт в ВК»**.

Как пользоваться: нажмите кнопку → скрипт спросит количество звонков
(чат спрашивается только в первый раз) → отчёт соберётся по тем же
правилам, что и в расширении (колонка «Результат», расшифровка полей,
сумма ДМХ, пустые значения = 0), скопируется в буфер обмена, и откроется
чат ВК. Останется нажать «вставить» в поле сообщения и отправить.

## Структура проекта

```
manifest.json   — манифест расширения (Manifest V3)
background.js   — service worker: расписание, сбор данных, ввод через DevTools
content.js      — поиск поля ввода ВК и кнопки отправки
mts.js          — сбор показателей со страницы agent_day.php
popup.html/js/css — окно настроек
mobile/mts-vk-report.user.js — скрипт Tampermonkey для телефона
icons/          — иконки
```
